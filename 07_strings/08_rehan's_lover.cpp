#include <iostream>
using namespace std;
int main() {
    
    // find rehan's love, one love, the one and only love, the LOML
    string s;
    cin >> s;
    if (s == "akshita" or s == "Akshita" or s == "AKSHITA") cout << "YES, she's the only one";
    else cout << "NO, fuck no";
    return 0;
}